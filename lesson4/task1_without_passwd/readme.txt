Что делал:

1. Восстановление с помощью LiveCD:

1.1 Добавил в виртуальную машину LiveCD, запустился
1.2 mount /mnt/sysimage
1.3 passwd
1.4. Выключил машину, удалил LiveCD, запустился - новый пароль сработал.

2. rd.break

2.1 Нажал 'e' в окне загрузки grub
2.2. Добавил rd.break в конец строки с linux16, заодно убрал rhgb и quiet
2.3. Также пришлось убрать console=ttyS0 
2.4. Появился промт командной строки
2.5. Перемонтировал sysroot - разрешил запись:
mount -o remount,rw /sysroot
2.6. chroot /sysroot
2.7. passwd
2.8. touch /.autorelabel
2.9. mount -o remount,ro /
2.10 exit, exit -> profit :)

3. подмена init

3.1. Вошёл в режим редактирования загрузки grub ('e')
3.2. В строке с linix16 поменял ro на rw и добавил init=/sysroot/bin/sh
3.3. Продолжил загрузку (ctrl + x)
3.4. chroot /sysroot
3.5. passwd root
3.6. touch /.autorelabel
3.7. exit
3.8. reboot 

.. но почему-то не взлетело. Новый пароль не срабатывает; старый, что интересно - тоже. Проделал всё это несколько раз - эффект тот же, т.е. вариант, что я сразу забыл пароль, можно исключить:)